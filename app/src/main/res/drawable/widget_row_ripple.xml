<?xml version="1.0" encoding="utf-8"?>
<!--
  Click feedback for widget rows. RemoteViews can't run custom animations, so the
  press animation is built from drawable states: a ripple plus a pressed-state
  overlay that fades in/out (enter/exitFadeDuration) for a clear tap animation.

  Layers are white so a runtime background tint (set to the app accent color in
  InboxWidgetService, API 31+) recolors the whole press animation to the accent.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#55FFFFFF">
    <item android:id="@android:id/mask">
        <color android:color="#FFFFFFFF" />
    </item>
    <item>
        <selector android:enterFadeDuration="120"
            android:exitFadeDuration="220">
            <item android:state_pressed="true">
                <color android:color="#33FFFFFF" />
            </item>
            <item>
                <color android:color="@android:color/transparent" />
            </item>
        </selector>
    </item>
</ripple>
