<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- MD3-inspired surface tokens (light mode) -->

    <!-- Action mode bar tint (overridden by accent at runtime) -->
    <color name="actionModeBlue">#1976D2</color>

    <!-- Destructive action (contact/event delete); same red the calendar editor uses -->
    <color name="contacts_delete_red">#E5484D</color>

    <!-- Ripple / state layer -->
    <!-- Settings grouped-list card, first frame only: applyTheme repaints it from
         ThemeTokens.surfaceCard. Fixed (not DayNight) so a light-mode system does
         not flash a light card under a dark app theme. -->
    <color name="settingsCardFallback">#49454F</color>
</resources>
